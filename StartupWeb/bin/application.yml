error:
  whitelabel:
    enabled: false

spring:
  datasource:
    url: jdbc:mysql://localhost:8889/startupwebdb
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect

        
